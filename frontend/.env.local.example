# Mapbox tile access token (used by the Sentry map)
NEXT_PUBLIC_MAPBOX_TOKEN=

# MoMo Sentry FastAPI backend (local or your Render URL)
NEXT_PUBLIC_MOMO_SENTRY_API=http://localhost:8000

# Supabase — same project as PAR-Map
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
